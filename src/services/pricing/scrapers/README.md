# Scrapers

Adapters in this folder fetch public price sources through explicit store/source modules. They must set a user agent, respect robots/ToS where applicable, apply rate limits, and return raw rows only. Normalization and matching live in sibling modules.
